#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;
    char g = '\n';
    cout << (a > b) << g;
    cout << (a > c) << g;
    cout << (a > d) << g;
    cout << (a > e) << g;
    return 0;
}